# Required for `summarize`, `embed`, and embedding-based `query`
OPENAI_API_KEY=your_key_here

# Optional overrides (defaults work for local dev under ./data)
# DB_PATH=data/docs.sqlite3
# RAW_DIR=data/raw_data
# MD_EXPORT_ROOT=data/openai_docs_split_rebuilt
# SITEMAP_PATH=data/sitemap.xml
